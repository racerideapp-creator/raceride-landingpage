# Cloudflare Pages redirects/rewrites

# Social-Bio-Links (302 Redirect zum Anker auf der Startseite)
/beta-tester /#beta-tester 302

# /live und /live/{token} werden von Cloudflare Pages Functions
# (functions/live/[token].js + functions/live/index.js) behandelt —
# hier absichtlich KEINE _redirects-Rewrite-Regel dafuer: Functions
# greifen vor der statischen Asset-Aufloesung UND vor _redirects,
# die haben keinen Namens-Overlap-Konflikt mit .html-Dateien im Root
# und keine Clean-URL-Kanonisierungs-Kollision (siehe Session-Log
# 2026-07-14: vier vorherige _redirects-Anlaeufe scheiterten daran).
